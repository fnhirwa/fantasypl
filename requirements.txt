# Core dependencies
numpy>=1.20.0
pandas>=1.3.0
requests>=2.26.0

# Legacy dependencies (from old version)
bs4
curl_cffi

# Note: Install optional dependencies with:
# pip install fplx[ml]        - for ML models
# pip install fplx[optimization] - for ILP optimizer
# pip install fplx[all]       - for everything
